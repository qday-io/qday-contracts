# Deployment Configuration
# ======================

# Required: Private key for deployment (with 0x prefix)
# For local testing, you can use Anvil's default private key:
PRIVATE_KEY=0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80

# For mainnet deployment, use your actual private key:
# PRIVATE_KEY=0xyour_actual_private_key_here_with_0x_prefix

# Optional: Admin address (defaults to deployer address if not set)
ADMIN_ADDRESS=0x1234567890123456789012345678901234567890

# Optional: Initial token supply (defaults to 0)
INITIAL_SUPPLY=0

# Optional: Whether to verify contract (defaults to true)
VERIFY_CONTRACT=true

# Network Configuration
# ====================

# Required: Ethereum Mainnet RPC URL
# Get from providers like Alchemy, Infura, or QuickNode
ETH_RPC_URL=https://eth-mainnet.g.alchemy.com/v2/your_api_key_here

# Gas Configuration
# ================

# Optional: Gas price in wei (defaults to 20 gwei for mainnet)
GAS_PRICE=20000000000

# Optional: Gas limit (defaults to 5,000,000)
GAS_LIMIT=5000000

# Block Explorer Configuration
# ===========================

# Optional: Etherscan API key for contract verification
# Get from https://etherscan.io/apis
ETHERSCAN_API_KEY=your_etherscan_api_key_here

# Other Configuration
# ==================

# Optional: Enable gas reporting (defaults to true)
REPORT_GAS=true

# Security Notes:
# ===============
# 1. Never commit your .env file to version control
# 2. Keep your private key secure and never share it
# 3. Use environment-specific API keys
# 4. Consider using a hardware wallet for mainnet deployments
# 5. The default private key above is only for local testing
